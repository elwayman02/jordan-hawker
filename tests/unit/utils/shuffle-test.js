import shuffle from 'jordan-hawker/utils/shuffle';
import { module, test } from 'qunit';

module('Unit | Utility | shuffle', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = shuffle();
    assert.ok(result);
  });
});
